# daymatch

여러 명이 되는 날짜를 표시해 공통으로 좋은 날을 찾는 웹앱.

## 기술 스택

- Vite + React (JavaScript, TypeScript 아님)
- UI/상태관리 라이브러리 최소화 — 꼭 필요한 경우가 아니면 추가하지 않음
- 예외로 도입 완료된 라이브러리: `react-router-dom`(라우팅), `@supabase/supabase-js`(데이터 저장소)

## 방(room) 개념

- 방마다 6자리 코드(헷갈리는 글자 0/O, 1/l/I 제외)를 부여
- 경로: `/r/{코드}` — 새 약속 만들기는 코드 생성 후 이 경로로 이동, 코드 입력 참여도 동일 경로로 이동
- 방마다 데이터가 완전히 분리됨 (같은 이름이라도 방이 다르면 별개)
- 이름도 방마다 따로 저장 (한 사람이 방 A에서는 "라미", 방 B에서는 "오가람"처럼 다르게 쓸 수 있음). 가장 최근에 쓴 이름은 별도로 기억해뒀다가 다음 방의 기본값으로 사용

## 데이터 구조 & 저장소

- 선택 데이터는 Supabase `selections` 테이블에 저장. 컬럼: `room_code` / `name` / `date`(`"YYYY-MM-DD"` 문자열) / `level`(`no`/`ok`/`good`)
- `(room_code, name, date)` unique 제약 — 저장은 upsert로 덮어쓰고, 선택 해제 시 해당 행을 삭제
- 화면에서 다루는 인메모리 구조는 기존과 동일하게 이름이 바깥 키인 형태 유지:
  ```js
  { "이름": { "2026-09-01": "good" } }
  ```
- 이름(내가 누군지 식별하는 값)만 예외적으로 `localStorage`에 저장 — 서버에 저장하지 않음
- 저장소를 다른 백엔드로 교체할 일이 생기면 `src/lib/calendarData.js`의 load/upsert/delete 함수 내부만 바꾸면 되도록 유지

## 상태 값

- `no` (바빠요) / `ok` (괜찮아요) / `good` (좋아요)

## 공개 범위 규칙

- 입력 화면(달력)에는 로그인한 본인의 선택만 보임 — 다른 사람이 뭘 찍었는지 노출하지 않음
- 다른 사람의 선택은 결과 화면에서만 집계된 형태로 공개

## 집계 규칙

- "안됨"은 점수에 합산하지 않고 그룹을 나누는 필터로만 사용
- 그룹: 전원 가능 / 1명만 빼고 가능 / 2명 이상 불가
- 그룹 내 정렬: 가능 인원 많은 순 → 좋아요 많은 순

## 색 규칙

- 입력 화면: 빨강(✕ 바빠요) / 초록(△ 괜찮아요) / 파랑(◎ 좋아요)
- 결과 화면: 파랑 농도(히트맵)만 사용
- 색만으로 구분하지 말 것 — 항상 기호(✕ △ ◎)를 함께 표시
- 라이트 테마 고정 — `prefers-color-scheme`(다크 모드)를 따라가지 않음. 항상 흰 배경 기준으로 색 대비를 확인할 것
- 새 색을 쓰거나 기존 색을 바꿀 때는 흰 배경(`#fff`) 대비 명도 대비를 확인 — 텍스트/기호는 4.5:1, 배경색 같은 비텍스트 요소는 3:1 이상을 목표로 함

## 표기 원칙

- 퍼센트 대신 개수로 표시
- 요약은 긍정형으로 (예: "가능 N명")
- 예외만 이름으로 명시 (예: 불가한 사람 이름)

## 네트워크 요청 처리

- 데이터를 Supabase에서 불러오거나 저장하는 곳은 반드시 로딩 상태와 에러 상태를 처리할 것
- 불러오는 중에는 로딩 표시, 실패하면 에러 메시지를 보여주고 화면이 그냥 죽거나 빈 상태로 방치되지 않게 할 것

## 반응형

- iPhone SE (375px) 기준으로 확인
- 터치 영역 최소 44px

## 작업 규칙

- 개발 서버는 사용자가 직접 띄워서 사용 중이므로 임의로 끄지 말 것
